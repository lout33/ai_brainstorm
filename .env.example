# AI Brainstorm - Environment Variables
# Copy this file to .env for local development

# ============================================
# DEPLOYMENT MODE
# ============================================

# Set to 'true' to enable proxy mode (serverless function handles API calls)
# Set to 'false' or leave unset for client-side mode (users provide their own API key)
# Default: false (client-side mode)
VITE_USE_PROXY=false

# ============================================
# PROXY MODE CONFIGURATION
# (Only required when VITE_USE_PROXY=true)
# ============================================

# OpenRouter API key for server-side requests
# Get your key at: https://openrouter.ai/keys
# REQUIRED in proxy mode, ignored in client-side mode
OPENROUTER_API_KEY=sk-or-v1-...

# Rate limiting for proxy mode
# Number of requests allowed per IP address per time window
# Default: 20
VITE_RATE_LIMIT_REQUESTS=20

# Rate limit time window in milliseconds
# Default: 3600000 (1 hour)
VITE_RATE_LIMIT_WINDOW_MS=3600000

# ============================================
# CLIENT-SIDE MODE
# (Default mode - no environment variables required)
# ============================================

# In client-side mode:
# - Users enter their own OpenRouter API key in the UI
# - API key is stored in browser localStorage
# - No server-side configuration needed
# - Perfect for open-source deployments where users bring their own keys

# ============================================
# DEPLOYMENT EXAMPLES
# ============================================

# Example 1: Client-side mode (simplest, recommended for open source)
# VITE_USE_PROXY=false
# (No other variables needed)

# Example 2: Proxy mode (for managed instances with rate limiting)
# VITE_USE_PROXY=true
# OPENROUTER_API_KEY=sk-or-v1-your-actual-key-here
# VITE_RATE_LIMIT_REQUESTS=20
# VITE_RATE_LIMIT_WINDOW_MS=3600000
